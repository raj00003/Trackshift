//! Generated protobuf bindings re-exported for convenient use across the agent.
#![allow(clippy::all, warnings)]

pub mod trackshift {
    pub mod chunkref {
        include!("gen/trackshift.chunkref.rs");
    }
    pub mod common {
        include!("gen/trackshift.common.rs");
    }
    pub mod decisionlog {
        include!("gen/trackshift.decisionlog.rs");
    }
    pub mod intent {
        include!("gen/trackshift.intent.rs");
    }
    pub mod jobs {
        include!("gen/trackshift.jobs.rs");
    }
    pub mod manifest {
        include!("gen/trackshift.manifest.rs");
    }
}
